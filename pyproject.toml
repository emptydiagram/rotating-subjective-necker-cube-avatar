[project]
name = "rotating-subjective-necker-cube-avatar"
version = "0.1.0"
description = "Rotating subjective necker cube avatar for bluesky"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "atproto>=0.0.61",
    "matplotlib>=3.10.1",
    "numpy>=2.2.5",
    "python-dotenv>=1.1.0",
]
